---
marp: true
theme: default
paginate: true
backgroundColor: #fff
style: |
  section {
    font-family: 'Pretendard', 'Noto Sans KR', sans-serif;
  }
  h1 {
    color: #2563eb;
  }
  h2 {
    color: #1e40af;
  }
  code {
    background-color: #f1f5f9;
  }
  table {
    font-size: 0.85em;
  }
---

# AI 코딩 도구 실전 가이드

## AI와 함께 애플리케이션을 만드는 워크플로우

---

# 목차

1. **Part 0**: 왜 이걸 알아야 하는가
2. **Part 1**: 배경 이론
3. **Part 2**: 개발 워크플로우
4. **Part 3**: 프롬프트 패턴
5. **Part 4**: 흔한 실수와 함정

---

# Part 0: 왜 이걸 알아야 하는가

---

## AI 코딩 도구의 현실

```
2023년: "AI가 코드를 짜준다고?"
2024년: "AI 없이 어떻게 코딩했지?"
2025년: "AI를 잘 쓰는 사람 vs 못 쓰는 사람"
```

### 핵심 메시지

> AI 코딩 도구는 이제 **선택이 아닌 필수**
> 하지만 **잘 쓰는 방법**을 모르면 오히려 생산성이 떨어진다

---

## 이 가이드의 목표

1. AI 코딩 도구가 **왜** 작동하는지 이해
2. AI와 함께 앱을 만드는 **워크플로우** 습득
3. 효과적인 **프롬프트 패턴** 학습
4. **흔한 실수**를 피하는 방법

---

# Part 1: 배경 이론

> "도구가 왜 작동하는지 알아야 잘 쓸 수 있다"

---

## 1.1 토큰 (Token)

LLM은 텍스트를 "토큰"이라는 조각으로 쪼개서 처리

```
"Hello, world!" → ["Hello", ",", " world", "!"]  (4 토큰)
"안녕하세요"     → ["안", "녕", "하", "세", "요"]  (5 토큰)
```

| 영향 | 설명 |
|------|------|
| **비용** | 토큰 수 = 비용. 한글은 영어보다 ~3배 더 많은 토큰 사용 |
| **속도** | 토큰이 많을수록 응답 시간 증가 |
| **한계** | 컨텍스트 윈도우 내에서만 처리 가능 |

---

## 1.2 컨텍스트 윈도우

LLM이 한 번에 "볼 수 있는" 텍스트의 최대 크기

### 비유: 책상 위 작업 공간

```
┌─────────────────────────────────────┐
│         컨텍스트 윈도우              │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │시스템│ │대화 │ │코드 │ │질문 │   │
│  │프롬프트│ │히스토리│ │파일 │ │    │   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
│  ⚠️ 꽉 차면 오래된 내용이 밀려남      │
└─────────────────────────────────────┘
```

---

## 모델별 컨텍스트 크기

| 모델 | 컨텍스트 | 비유 |
|------|----------|------|
| GPT-3.5 | 16K 토큰 | A4 용지 20장 |
| GPT-4 | 128K 토큰 | 책 한 권 |
| Claude 3.5 | 200K 토큰 | 두꺼운 논문집 |
| Gemini 2.5 | 1M 토큰 | 도서관 한 섹션 |

### 실전 팁
- 긴 대화 후 AI가 처음 지시를 잊는다면? → **컨텍스트 오버플로우**
- 해결책: 중요한 지시는 다시 언급하거나, 새 대화 시작

---

## 1.3 Temperature (창의성 다이얼)

AI 응답의 "무작위성"을 조절하는 설정

```
Temperature 0.0  ─────────────────────  Temperature 1.0
     │                                        │
   예측 가능                                창의적
   항상 같은 답                            매번 다른 답
   코드 생성에 적합                         브레인스토밍에 적합
```

| 작업 | 권장 Temperature |
|------|-----------------|
| 코드 생성 | 0.0 ~ 0.3 |
| 버그 수정 | 0.0 |
| 아이디어 발상 | 0.7 ~ 1.0 |
| 문서 작성 | 0.3 ~ 0.5 |

---

## 1.4 핵심 패턴: RAG

### Retrieval-Augmented Generation

**비유**: 오픈북 시험

```
일반 LLM:        "내가 아는 것만으로 답할게" (환각 위험)
RAG 적용 LLM:    "관련 문서 찾아보고 답할게" (정확도 향상)
```

**동작 방식**:
```
질문 → 관련 문서 검색 → 검색 결과 + 질문 → LLM → 답변
```

---

## 1.4 핵심 패턴: Function Calling

### Tool Use

**비유**: 매니저와 직원들

```
사용자: "서울 날씨 알려줘"

LLM (매니저): "이건 내가 직접 못해. 날씨 API 호출해야겠다"
     ↓
Tool (직원): get_weather("서울") → {"temp": 15, "condition": "맑음"}
     ↓
LLM: "서울은 현재 15도이고 맑습니다"
```

---

## 1.4 핵심 패턴: Agent

**비유**: 자율적인 인턴

```
일반 LLM:  질문 → 답변 (1회성)

Agent:     목표 → 계획 → 실행 → 관찰 → 계획 수정 → ... → 완료
```

### Agent의 동작 루프 (ReAct)

```
1. Think:   "무엇을 해야 하지?"
2. Act:     도구 실행 (파일 읽기, 검색, 코드 실행)
3. Observe: 결과 확인
4. Repeat:  목표 달성까지 반복
```

---

## 1.4 핵심 패턴: MCP

### Model Context Protocol

**비유**: USB-C 표준

```
Before MCP:  각 도구마다 다른 연결 방식 (혼란)
After MCP:   표준화된 연결 방식 (일관성)
```

AI가 외부 도구와 통신하는 **표준 프로토콜** (Anthropic 개발)

- 한 번 만든 MCP 서버는 모든 MCP 지원 AI에서 사용 가능
- 사내 시스템을 AI와 쉽게 연결 가능

---

## 1.5 컨텍스트 관리 전략

> "컨텍스트는 AI의 작업 기억이다. 관리하지 않으면 넘친다."

### 문제 상황

```
컨텍스트 윈도우가 꽉 차면:
→ 💥 처음 지시사항 잊어버림
→ 💥 같은 실수 반복
→ 💥 엉뚱한 파일 수정
```

---

## 전략 1: 서브에이전트로 작업 분리

큰 작업을 독립적인 작은 작업으로 나누어 별도 컨텍스트에서 실행

```
✅ 서브에이전트 패턴
┌─────────────────┐
│   메인 에이전트   │ ← 전체 조율, 결과 통합
└────────┬────────┘
         │
    ┌────┴────┬─────────┬─────────┐
    ▼         ▼         ▼         ▼
┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐
│ 분석   │ │ 구현   │ │ 테스트 │ │ 문서화 │
└───────┘ └───────┘ └───────┘ └───────┘
  독립적 컨텍스트 (각각 깨끗한 상태로 시작)
```

---

## 서브에이전트 분리 기준

| 기준 | 설명 | 예시 |
|------|------|------|
| **독립성** | 다른 작업 결과에 의존하지 않음 | 파일 A 분석 vs 파일 B 분석 |
| **범위** | 명확한 입력/출력 정의 가능 | "이 함수의 버그 찾기" |
| **컨텍스트 크기** | 단일 작업이 컨텍스트의 30% 이하 | 파일 5개 이하 |

---

## 실전 예시: 대규모 리팩토링

```
Step 1: 메인 에이전트 - 계획 수립
"src/ 폴더의 구조를 분석하고 리팩토링 계획을 세워줘"

Step 2: 서브에이전트들 - 분석 위임
├─ 서브 1: "src/api/ 폴더 분석해서 의존성 맵 만들어줘"
├─ 서브 2: "src/utils/ 폴더에서 중복 코드 찾아줘"
└─ 서브 3: "src/components/에서 미사용 컴포넌트 찾아줘"

Step 3: 메인 에이전트 - 결과 통합
"각 분석 결과를 바탕으로 리팩토링 우선순위 정해줘"

Step 4: 서브에이전트들 - 구현 위임
├─ 서브 4: "UserService 클래스 리팩토링해줘"
└─ 서브 5: "중복된 날짜 유틸 함수 통합해줘"
```

---

## 전략 2: 컨텍스트 스냅샷

중요한 정보를 별도 파일로 저장하고 필요할 때 참조

```markdown
# context-snapshot.md

## 현재 작업
- 목표: 결제 시스템 리팩토링
- 진행 상황: Step 2/5 완료

## 핵심 결정사항
- PaymentService는 인터페이스로 추상화
- 기존 API 엔드포인트 유지 (하위 호환성)

## 건드리면 안 되는 것
- /api/v1/* 라우트 시그니처
```

**활용**: "context-snapshot.md를 읽고 이어서 작업해줘"

---

## 전략 3: 컨텍스트 프리로딩

작업 시작 전에 필요한 정보만 선별적으로 로드

```
❌ 나쁜 예: "전체 프로젝트 분석해줘"
→ 모든 파일 로드 시도 → 오버플로우

✅ 좋은 예: 단계적 로드
1. "package.json과 폴더 구조만 보여줘"
2. "src/services/ 폴더의 파일 목록만 보여줘"
3. "PaymentService.ts 파일만 읽어줘"
4. "이 파일에서 호출하는 다른 파일들 목록 알려줘"
```

---

## 컨텍스트 관리 체크리스트

**작업 시작 전**:
- [ ] 작업 범위가 컨텍스트의 50% 이하인가?
- [ ] 서브에이전트로 분리 가능한 작업인가?

**작업 중**:
- [ ] AI가 지시를 잊는 징후? → 새 대화 시작
- [ ] 파일 10개 이상 읽음? → 컨텍스트 스냅샷 생성
- [ ] 대화 50턴 이상? → 요약 요청

**작업 완료 후**:
- [ ] 컨텍스트 스냅샷 저장
- [ ] 주요 결정사항 문서화

---

# Part 2: 개발 워크플로우

> "AI와 함께 앱 만드는 4단계"

---

## 워크플로우 개요

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1. SPEC    │ →  │  2. DEVELOP │ →  │  3. TEST    │ →  │  4. VERIFY  │
│   (문서화)   │    │    (개발)    │    │   (테스트)   │    │    (검증)   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

---

## 2.1 SPEC (문서화)

### 핵심 원칙

> **"AI는 모호함을 채우는 컴파일러"**
> 스펙이 모호하면 AI가 마음대로 해석한다. 그리고 그 해석은 대부분 틀리다.

### 역할 분담

| Human | AI |
|-------|-----|
| 요구사항 정의 | 스펙 작성 보조 |
| 비즈니스 로직 결정 | 빠진 부분 찾기 |
| 최종 승인 | 모호함 지적 |

---

## 좋은 스펙 구조

```markdown
## 1. 목표 (Goal)
## 2. 비목표 (Non-Goals)  ← 매우 중요!
## 3. 유저 스토리
## 4. 기능 요구사항
## 5. 엣지 케이스
## 6. 성공 기준
```

---

## 나쁜 스펙 vs 좋은 스펙

### ❌ 나쁜 스펙
```
사용자가 리포트를 내보낼 수 있어야 함
```

### ✅ 좋은 스펙
```markdown
## 리포트 내보내기
### 지원 포맷: PDF (A4, 세로), CSV (UTF-8)
### 제약 조건: 10,000행 이하 5초 이내 완료
### 엣지 케이스: 데이터 없음 → 메시지 표시
```

---

## 2.2 DEVELOP (개발)

### 핵심 원칙

> **"AI는 주니어 개발자"**
> 빠르게 코드를 만들어내지만, 결과물은 반드시 시니어가 리뷰해야 한다.

### 역할 분담

| Human | AI |
|-------|-----|
| 아키텍처 결정 | 코드 생성 |
| 코드 리뷰 | 보일러플레이트 작성 |
| 패턴 가이드 | 반복 작업 |

---

## 프로젝트 컨텍스트: CLAUDE.md

```markdown
# 프로젝트 개요
Next.js 14 App Router + TypeScript + Tailwind CSS

# 코딩 컨벤션
- 함수형 컴포넌트만 사용
- Props는 interface로 정의

# 금지 패턴
- any 타입 사용 금지
- console.log 프로덕션 코드에 금지
```

---

## 개발 순서

```
1. 스펙을 작은 단위로 분할
   └─ "전체 기능 만들어줘" (❌)
   └─ "로그인 폼의 validation 로직만 만들어줘" (✅)

2. 기존 코드 패턴 보여주기
   └─ "이 프로젝트의 다른 폼 컴포넌트처럼 만들어줘"

3. 제약 조건 명시
   └─ "기존 API 인터페이스는 변경하지 마"

4. 생성된 코드 리뷰
   └─ 모든 코드를 직접 읽고 이해
```

---

## 2.3 TEST (테스트)

### 핵심 원칙

> **"AI가 만든 테스트도 틀릴 수 있다"**
> 테스트가 정말로 의도한 동작을 검증하는지 사람이 확인해야 한다.

### 역할 분담

| Human | AI |
|-------|-----|
| 테스트 의도 정의 | 테스트 코드 생성 |
| assertion 검증 | 엣지 케이스 확장 |
| 테스트 품질 확인 | 보일러플레이트 작성 |

---

## TDD with AI

```
1. Human: 테스트해야 할 동작 정의
   "로그인 시 잘못된 비밀번호면 에러 메시지 표시"

2. AI: 테스트 코드 생성
   test("shows error on wrong password", ...)

3. Human: 테스트 로직 검증
   "이 테스트가 정말 에러 메시지를 확인하나?"

4. AI: 테스트 통과하는 코드 구현

5. CI: 테스트 실행 + mutation testing
```

---

## 2.4 VERIFY (검증)

### 핵심 원칙

> **"AI 리뷰는 1차 필터, 사람 리뷰가 최종"**
> AI는 명백한 실수를 빠르게 잡고, 사람은 비즈니스 로직과 의도를 검증한다.

---

## 검증 레이어

```
┌────────────────────────────────────┐
│  Layer 1: AI 자동 리뷰              │
│  - 린트 에러, 타입 에러, 명백한 버그   │
└────────────────────────────────────┘
                ↓
┌────────────────────────────────────┐
│  Layer 2: Human 리뷰               │
│  - 비즈니스 로직, 코드 의도          │
└────────────────────────────────────┘
                ↓
┌────────────────────────────────────┐
│  Layer 3: CI 자동화                │
│  - 테스트 실행, 타입 체크, 빌드       │
└────────────────────────────────────┘
```

---

# Part 3: 프롬프트 패턴

> "좋은 프롬프트 = 좋은 결과"

---

## 3.1 기본 원칙

| 원칙 | 나쁜 예 | 좋은 예 |
|------|--------|--------|
| **구체적으로** | "이거 고쳐줘" | "Line 42의 null 체크 누락 수정해줘" |
| **제약 조건 명시** | "리팩토링해줘" | "public API 유지하고 내부만 정리해줘" |
| **단계별 요청** | "전체 기능 만들어줘" | "1단계: 타입 정의, 2단계: API, 3단계: UI" |
| **역할 부여** | (없음) | "시니어 개발자로서 리뷰해줘" |
| **출력 형식 지정** | (없음) | "JSON으로 응답해줘" |

---

## 3.2 버그 수정 템플릿

```markdown
## 문제
[예상 동작] vs [실제 동작]

## 에러 메시지
[정확한 에러 메시지 / 스택 트레이스]

## 관련 코드
[코드 붙여넣기]

## 이미 시도한 것
- 시도 1: [결과]

## 요청
1. 먼저 근본 원인을 분석해줘
2. 최소한의 수정안을 제시해줘
```

---

## 3.2 새 기능 구현 템플릿

```markdown
## 스펙
[기능 설명]

## 제약 조건
- [제약 1]
- [제약 2]

## 참고할 기존 코드
[비슷한 기존 구현 경로]

## 요청
1. 먼저 구현 계획을 설명해줘
2. 내가 승인하면 코드 작성해줘
3. 한 번에 전체 말고, 단계별로 진행해줘
```

---

## 3.3 고급 패턴

### Chain of Thought (단계별 사고)

```
이 문제를 단계별로 분석해줘:
1. 먼저 문제를 정확히 이해했는지 확인
2. 가능한 원인들을 나열
3. 각 원인의 가능성을 평가
4. 가장 가능성 높은 원인에 대한 해결책 제시

각 단계를 명시적으로 보여줘.
```

---

## 3.3 고급 패턴

### Few-shot (예시 제공)

```
다음 형식으로 에러 메시지를 작성해줘:

예시 1:
입력: 이메일 형식 오류
출력: "올바른 이메일 주소를 입력해주세요."

예시 2:
입력: 비밀번호 길이 부족
출력: "비밀번호는 최소 8자 이상이어야 합니다."

이제 작성해줘:
입력: 전화번호 형식 오류
```

---

## 3.3 고급 패턴

### 역할 부여 (Role Prompting)

```
너는 10년차 시니어 백엔드 개발자야.
보안과 성능에 특히 민감하고,
코드 리뷰할 때 꼼꼼하게 본다.

다음 코드를 리뷰해줘:
[코드]
```

---

# Part 4: 흔한 실수와 함정

---

## 4.1 AI 맹신

| 실수 | 결과 | 해결책 |
|------|------|--------|
| AI 답변 무조건 수용 | 버그, 보안 취약점 | 모든 코드 직접 리뷰 |
| "AI가 맞다고 했으니까" | 책임 회피 불가 | 최종 책임은 개발자 |

### 명심할 것

> AI는 **"그럴듯하게 틀릴 수 있다"** (Confidently Wrong)

---

## 4.2 컨텍스트 부족

| 실수 | 결과 | 해결책 |
|------|------|--------|
| 코드만 던지기 | 엉뚱한 수정 | 배경, 제약조건 함께 제공 |
| 에러 메시지 생략 | 추측성 답변 | 정확한 에러 복붙 |

### 좋은 컨텍스트 체크리스트
- [ ] 무엇을 하려고 했나?
- [ ] 무엇이 잘못됐나?
- [ ] 정확한 에러 메시지는?
- [ ] 어떤 제약 조건이 있나?

---

## 4.3 범위 관리 실패

| 실수 | 결과 | 해결책 |
|------|------|--------|
| "전체 앱 만들어줘" | 품질 저하 | 작은 단위로 분할 |
| 한 번에 너무 많은 요청 | 일부만 처리됨 | 하나씩 순차적으로 |

### 좋은 요청 크기
```
❌ "로그인 시스템 전체 만들어줘"
✅ "로그인 폼의 이메일 validation 로직 만들어줘"
```

---

## 4.4 학습 기회 상실

| 실수 | 결과 | 해결책 |
|------|------|--------|
| 복붙만 하기 | 실력 정체 | "왜 이렇게 했어?" 물어보기 |
| 이해 없이 사용 | 디버깅 불가 | 코드 설명 요청 |

### 성장하는 AI 활용법
1. AI가 코드 생성
2. "이 코드가 뭘 하는지 설명해줘"
3. "왜 이 방식을 선택했어?"
4. 이해한 후 적용

---

## 4.5 보안 실수

| 실수 | 위험 | 해결책 |
|------|------|--------|
| API 키 프롬프트에 포함 | 키 노출 | 환경 변수 사용 |
| 민감 코드 공유 | 정보 유출 | 사내 보안 정책 준수 |

### 절대 프롬프트에 포함하지 말 것
- API 키, 비밀번호
- 고객 개인정보
- 사내 기밀 로직

---

# 체크리스트

---

## 개발 시작 전
- [ ] 스펙 문서 작성 완료
- [ ] 비목표 명시됨
- [ ] 엣지 케이스 정의됨
- [ ] 프로젝트 컨텍스트 파일 설정 (`CLAUDE.md`)

## 개발 중
- [ ] 작은 단위로 요청
- [ ] 제약 조건 명시
- [ ] 생성된 코드 직접 리뷰
- [ ] 기존 패턴과 일관성 확인

---

## 테스트 시
- [ ] 테스트 의도 명확
- [ ] assertion이 실제로 검증하는지 확인
- [ ] 엣지 케이스 포함
- [ ] CI 통과

## 완료 전
- [ ] 스펙 대비 구현 검증
- [ ] 코드 리뷰 완료
- [ ] 보안 체크
- [ ] 문서 업데이트

---

# 마무리

AI 코딩 도구는 **생산성을 크게 높여주는 강력한 도구**다.

하지만 잘 쓰려면:
1. **원리를 이해**하고 (Part 1)
2. **체계적인 워크플로우**를 따르고 (Part 2)
3. **효과적인 프롬프트**를 사용하고 (Part 3)
4. **흔한 실수를 피해야** 한다 (Part 4)

---

# 핵심 메시지

> **"AI가 코드를 짜주는 시대.**
> **하지만 좋은 코드는 여전히 사람의 판단에서 나온다."**

---

# 감사합니다

## 참고 자료
- 이 프레젠테이션의 전체 내용: `README.md`
- Marp: https://marp.app

### 사용법
```bash
# PDF로 변환
npx @marp-team/marp-cli presentation.md --pdf

# HTML로 변환
npx @marp-team/marp-cli presentation.md --html

# PPTX로 변환
npx @marp-team/marp-cli presentation.md --pptx
```
